{% extends 'base.html.twig' %}

{% block title %}Update Comment{% endblock %}

{% block stylesheets %}
    {{ encore_entry_link_tags('app.registration') }}
{% endblock %}

{% block body %}

<div class="container">
    <div class="disconnect-form">
        <h1 class="hide-block media-title-two mt-4">Mise à jour - Commentaire</h1>

        {{ form_start(form) }}
            <div class="border-form">
                <div class="form-group">
                    {{ form_row(form.text, {'label': 'Votre Commentaire'}) }}
                </div>
                <div class="d-flex flex-column align-items-center">
                    <button type="submit" class="btn btn-success re-button">Soumettre</button>
                    <a href="{{ path('trick_action', {slug: comment.trick.slug}) }}">Revenir sur la page du Trick</a>
                </div>
            </div>
        {{ form_end(form) }}
    </div>
</div>

{% endblock %}
